# Agent 작업 가이드 (AGENT_README.md)

이 문서는 **GB4P_ubuntu_my_preferences** 프로젝트에서 작업하는 AI Agent를 위한 지침서입니다.
새로운 설정을 추가하거나 기존 스크립트를 수정할 때 본 문서의 규칙을 반드시 준수해야 합니다.

---

## 1. 프로젝트 목적 및 성격
- **목적**: 삼성 갤럭시 북4 프로(Galaxy Book 4 Pro, GB4P)에서 우분투를 사용하며 개인적으로 분석·패치하여 최적화한 설정들을, **추후 우분투 재설치 시 원클릭으로 간편하게 복원**하기 위한 자동화 스크립트 모음입니다.
- **성격**: 하드웨어 특화 튜닝(터치패드, 키보드, 디스플레이, 전원 관리 등), 개인 개발 환경 및 시스템 환경설정을 모듈 단위로 관리합니다.

---

## 2. Agent 핵심 필수 규칙 (CRITICAL)

### 📌 규칙 1. 스크립트 작성 시 설명 마크다운 파일 동시 생성 (`<script_name>_README.md`)
새로운 패치/설정 복원 스크립트를 저장하거나 대규모로 수정할 때는 **반드시 해당 스크립트의 상세 동작과 설정을 설명하는 마크다운 문서를 함께 저장**해야 합니다.

- **파일명 규칙**: `<script_name>_README.md`
  - 예시: `touchpad_keyboard.sh` 작성 시 ➡️ `touchpad_keyboard.sh_README.md` 함께 작성
  - 예시: `display_tuning.sh` 작성 시 ➡️ `display_tuning.sh_README.md` 함께 작성
- **설명 마크다운 문서 필수 포함 내용**:
  1. **배경 및 목적**: 해당 패치가 왜 필요한지 (기본 우분투/하드웨어에서 발생하는 문제점)
  2. **수정/생성되는 시스템 경로**: 설정이 배치되는 절대 경로 (예: `/etc/libinput/...`, `/etc/keyd/...`, `~/.config/...`)
  3. **주요 파라미터 및 원리**: 설정값의 의미와 동작 메커니즘
  4. **사전 요구사항 및 의존성**: 필요한 패키지, 서비스 등
  5. **수동 확인 및 롤백(원상 복구) 방법**: 패치를 수동으로 되돌리는 방법 및 트러블슈팅

### 📌 규칙 2. 모듈형 독립 실행 보장
- 각 기능별 스크립트는 다른 스크립트에 종속되지 않고 **단독으로 실행**해도 완벽하게 복원되어야 합니다.
- 스크립트 명명은 성격을 알기 쉽게 스네이크 케이스로 작성합니다 (예: `touchpad_keyboard.sh`, `power_saving.sh` 등).

### 📌 규칙 3. 멱등성(Idempotency) 및 안전성
- 스크립트를 여러 번 연속으로 실행하더라도 시스템이 손상되거나 중복 설정되지 않아야 합니다.
- 관리자 권한이 필요한 경우 스크립트 최상단에서 `$EUID` 체크 후 필요 시 `sudo` 승격을 유도하도록 작성합니다.
- 설정 파일 덮어쓰기 전 필요에 따라 백업 또는 명확한 출력을 제공합니다.

---

## 3. 디렉토리 구조 규칙

```text
GB4P_ubuntu_my_preferences/
├── AGENT_README.md                # Agent 작업 지침서 (본 문서)
├── README.md                      # 사용자용 전체 프로젝트 개요 문서
├── <feature_name>.sh              # 기능별 원클릭 복원 스크립트
├── <feature_name>.sh_README.md   # [필수] 해당 스크립트의 상세 설명 문서
├── configs/                       # 복원에 필요한 원본 설정 파일들
│   └── <category>/                # 카테고리별 분류 (예: libinput, keyd, systemd)
└── scripts/                       # 세부 서브 스크립트 (모듈화가 필요한 경우)
```

---

## 4. 커밋 및 마무리 체크리스트
에이전트는 작업을 마친 후 다음 사항을 확인해야 합니다:
1. `bash -n <script_name>.sh`: 쉘 스크립트 문법 오류가 없는가?
2. `<script_name>.sh_README.md`가 빠짐없이 작성되었는가?
3. 프로젝트 메인 `README.md`의 목록/구조에 새로 추가된 스크립트가 반영되었는가?
4. git commit을 명확한 메시지와 함께 생성했는가?
