# ==============================================================================
# Kernel NMI Watchdog Power Saving Configuration
# ==============================================================================
# Disable NMI watchdog timer to prevent periodic interrupts from waking CPU cores,
# allowing deeper Package C-states (C8/C10) during idle.
kernel.nmi_watchdog = 0
