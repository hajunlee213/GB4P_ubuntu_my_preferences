# Agent Session Startup Rule

On new session start:
1. Read `AGENT_README.md` and `README.md`.
2. Inspect the conversation ID provided by user to locate the patch history.
3. Start creating the patch restore script (`<script_name>.sh`) and documentation (`<script_name>.sh_README.md`).
